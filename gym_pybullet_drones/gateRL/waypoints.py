import numpy as np


waypoints1 = np.array([
    [
        np.array([2, 0, 3]),
        np.array([1, 0, 3]),
        np.array([0, 1, 3]),
        np.array([-1, 0, 3]),
        np.array([0, -1, 3]),
        np.array([1, 0, 3]),
    ],
    [
        np.array([0, 0, np.pi]),
        np.array([0, 0, np.pi/4 * 3]),
        np.array([0, 0, np.pi]),
        np.array([0, 0, -np.pi/2]),
        np.array([0, 0, 0]),
        np.array([0, 0, np.pi/2]),
    ]    
])

waypoints2 = np.array([
    [
        np.array([2, 0, 3]),
        np.array([1, 0, 3]),
        np.array([0, 1, 3]),
        np.array([-1, 0, 3]),
        np.array([0, -1, 3]),
        np.array([1, 0, 3]),
    ],
    [
        np.array([0, 0, np.pi]),
        np.array([0, 0, np.pi/4 * 3]),
        np.array([0, 0, np.pi]),
        np.array([0, 0, -np.pi/2]),
        np.array([0, 0, 0]),
        np.array([0, 0, np.pi/2]),
    ]
])
